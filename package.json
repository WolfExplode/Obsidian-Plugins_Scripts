{
	"name": "excalidraw-pureref",
	"version": "1.1.0",
	"description": "Turn an Excalidraw board into a always-on-top PureRef-style reference popout with F11.",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc --noEmit --skipLibCheck && node esbuild.config.mjs production",
		"check": "tsc --noEmit --skipLibCheck && node --check main.js",
		"test": "node tests/run.mjs",
		"verify": "npm run build && node verify-build.mjs && npm test",
		"version": "node version-bump.mjs && git add manifest.json versions.json"
	},
	"keywords": [],
	"author": "",
	"license": "AGPL-3.0",
	"devDependencies": {
		"@types/node": "^16.11.6",
		"esbuild": "0.17.3",
		"obsidian": "latest",
		"tslib": "2.4.0",
		"typescript": "4.7.4"
	}
}
